window.LAGER_CONFIG = {
  // Kann leer bleiben und später in der App unter Einstellungen eingetragen werden.
  clientId: "",
  tenant: "common",
  graphScopes: ["User.Read", "Files.ReadWrite.AppFolder"]
};
